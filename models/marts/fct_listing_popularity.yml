models:
  - name: fct_listing_popularity
    columns: 
      - name: listing_id
        tests:
          - unique
          - not_null

          - relationships:
              arguments:
                to: ref('dim_listings')
                field: listing_id
